import 'package:flutter/material.dart';

class AgeBody extends StatelessWidget {
  const AgeBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('GenderBody'),
    );
  }
}
